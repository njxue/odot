import { Input } from "@chakra-ui/react";
import { useState, useRef } from "react";

export const Search: React.FC<{ handleChange: () => void }> = (props) => {
  const inputRef = useRef<HTMLInputElement>(null);

  return <></>;
};
